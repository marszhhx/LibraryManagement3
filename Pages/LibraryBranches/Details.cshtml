@page
@model LibraryManagement3.Pages.LibraryBranches.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<section class="mb-4">
    <div class="card">
        <div class="card-header text-center py-3">
            <h5 class="mb-0 text-center">
                <strong>Branch Details</strong>
            </h5>
        </div>
        <div class="card-body">
            <div class="col-md-7">
                <div>
                    <dl class="row">
                        <dt class="col-sm-4">
                            @Html.DisplayNameFor(model => model.LibraryBranch.BranchName)
                        </dt>
                        <dd class="col-sm-7">
                            @Html.DisplayFor(model => model.LibraryBranch.BranchName)
                        </dd>
                    </dl>
                </div>
                <div>
                    <a sp-page="./Edit" asp-route-id="@Model?.LibraryBranch.LibraryBranchId" class="btn btn-primary">Edit</a>
                    <hr/>
                    <a asp-page="./Index">Back to List</a>
                </div>
            </div>
        </div>
    </div>
</section>
